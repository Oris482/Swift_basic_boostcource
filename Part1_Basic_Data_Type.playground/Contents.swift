import Swift

//Bool
//true, false 값을 가질 수 있음
//0, 1은 오류 발생!

//Int, UInt
//둘다 현재는 기본적으로 64비트
//서로 간에 자료 교환은 불가능
//UInt 값이 Int값 범위 안에 있는 경우도 불가능

//Float, Double
//Float는 32비트 부동소수형, Double은 64비트 부동소수형
//실수는 물론 정수값도 받을 수 있음
//역시 서로 간에 자료 교환은 불가능

//Character, String
//유니코드 값을 가진 어떤 문자든 입력 가능
//큰따옴표 사이에 값을 넣어서 사용
//역시 서로 간에 자료 교환은 불가능
